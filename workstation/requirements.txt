httpx==0.28.1
websockets==16.1

# ASR is installed into its separately reviewed GPU environment after a real
# Cantonese benchmark. The manager imports it only when ASR is enabled.
# faster-whisper intentionally has no automatic install/pull path here.
