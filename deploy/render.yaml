services:
  - type: web
    name: skhlmc-dbt-marksys
    env: docker
    plan: starter
    dockerfilePath: ./deploy/Dockerfile
    # Production follows main. Any push to main can trigger a live deployment.
    autoDeploy: true
